try:
   a=input().lower()
   b=input().lower()
   if len(a)==len(b): 
      if sorted(a)==sorted(b):
         print("It's Anagram")
   else:
      print("It's not a Anagram")
except ValueError:
   print("Invalid value")
